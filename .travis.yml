sudo: required
dist: trusty
group: deprecated-2017Q4
language: python
python:
    - "3.4"
    - "3.5"
    - "3.6"

install:
    - pip3 install -r requirements.txt
script:
    - sudo python -c "import kickthemout;"

branches:
  only:
  - master

addons:
  apt:
    packages:
      - nmap

notifications:
  email:
    on_success: never
    on_failure: always